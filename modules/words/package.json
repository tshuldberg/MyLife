{
  "name": "@mylife/words",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "peerDependencies": {
    "@mylife/module-registry": "workspace:*"
  },
  "devDependencies": {
    "@mylife/typescript-config": "workspace:*",
    "codeflash": "^0.9.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
